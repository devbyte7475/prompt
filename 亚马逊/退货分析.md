# 商品退货分析标准化SOP（通用版·修订）

## 0. 文档说明

### 0.1 适用范围
本SOP适用于大部分实物商品品类的退货分析，包括但不限于服装、鞋靴、箱包、家居、美妆、个护、消费电子、运动户外、母婴、玩具等。

分析仅基于亚马逊退货报告内可用字段执行，不依赖外部销量、成本、广告、库存等数据。因此，分析结果以“退货内部结构、退货原因分布、不可售损失结构、问题商品/属性定位”为主，**不计算退货率、损失金额、利润影响**。

### 0.2 分析目标
1. 建立整体退货认知，识别退货规模、趋势与异常。
2. 定位高退货品类、高退货/高损失商品、异常属性。
3. 还原客户真实退货痛点，修正系统原因偏差。
4. 区分可售退货与不可售退货的动因差异。
5. 输出可执行的改善建议清单。

### 0.3 核心分析口径
- **退货单量**：以退货报告中的退货单/订单唯一标识去重计数；若无唯一标识，则按“日期 + SKU + 仓库”组合近似去重，需在报告中说明。
- **退货件数**：以`quantity`字段汇总；若`quantity > 1`，按件数统计。
- **主分析单位**：除特别说明外，均以**退货件数**为统计口径。
- **时间范围**：以`return-date`为准，统一为`YYYY-MM-DD`格式。

---

## 一、数据预处理阶段

### 1.1 数据导入与质量校验

1. **字段完整性检查**  
   - 保留核心字段：`return-date`、`sku`、`product-name`、`quantity`、`fulfillment-center-id`、`detailed-disposition`、`reason`、`status`、`customer-comments`。  
   - 如存在`order-id`、`return-id`、`asin`、`category`等字段，应一并保留。  
   - 核心字段缺失率超过5%时，需反馈数据源，不强行分析。

2. **去重与异常处理**  
   - 按`return-id/order-id + sku + return-date`组合去重，避免同一行重复导入。  
   - 对`quantity <= 0`、日期格式异常、`sku`为空的行，单独标记为“异常数据”，不计入主体分析，但需在数据质量报告中体现。  
   - 同一退货单包含多个SKU时，保持原记录，不合并SKU。

3. **空值处理**  
   - `customer-comments`为空：标记为「无用户评论」，保留但不参与评论语义分析。  
   - `reason`为空：标记为「未填写原因」，在原因分布中单独统计。  
   - 其他核心字段为空：单独标记，不删除。

4. **日期标准化**  
   - 将`return-date`统一转换为`YYYY-MM-DD`格式。  
   - 新增`year-month`、`week`字段，用于周/月聚合分析。

### 1.2 商品维度拆解：品类与产品父体

#### 1.2.0 SKU解析规则确认（前置步骤）

**重要：** 每个数据表中的SKU编码规则可能不同，不能预设统一规则自动拆分。在开始SKU解析前，必须先向数据提供方/用户确认以下信息：

1. SKU的组成结构：哪些部分代表产品父体/款号/型号，哪些代表变体属性（颜色、尺码、容量等）。  
2. 分隔符类型：`-`、`_`、空格或其他符号。  
3. 各部分的排列顺序及固定长度/变长规则。  
4. 是否存在特殊命名或例外情况。  

**执行原则：**
- 若用户提供了明确的SKU解析规则，则按规则执行解析。  
- 若用户无法提供规则，则**不进行SKU自动拆解**，只保留原始SKU字段，后续分析可按SKU粒度进行，或在报告中说明无法解析。  
- 严禁在未确认规则的情况下，凭经验猜测SKU含义。

#### 1.2.1 品类归类

1. **优先使用退货报告中的类目字段**  
   若报告中存在`category`、`department`、`product-type`等字段，优先使用该字段作为品类归属。

2. **无类目字段时，从`product-name`提取品类关键词**  
   - 建立**品类关键词库**，覆盖常见品类。  
   - 同一标题包含多个品类关键词时，按预设优先级归类。  
   - 示例优先级：  
     `大家电 > 厨房电器 > 个护电器 > 数码配件 > 服装 > 鞋靴 > 箱包 > 家居 > 美妆 > 玩具 > 运动户外 > 母婴`  
     > 具体优先级可根据业务实际调整。

3. **未分类处理**  
   无法匹配任何关键词的标题，标记为「未分类」，单独统计占比。若未分类占比超过10%，需扩充关键词库。

#### 1.2.2 SPU/父体/型号提取

1. **基于已确认的SKU规则提取**  
   - 按用户提供的规则，从SKU中提取产品父体/款号/型号标识。  
   - 示例：若规则为“第一个`-`前为父体”，则`TYD074-BK-M`的父体为`TYD074`。

2. **若SKU规则未确认，但存在`asin`或`product-name`相似聚合条件**  
   - 可使用`asin`直接作为产品唯一标识。  
   - 或基于`product-name`的相似度（如去除颜色、尺码等属性词后）进行聚合，但需在报告中说明该方法是近似处理，非精确SPU。

3. **无法解析的产品单独标记**  
   - 不强行归入某个SPU，避免错误聚合。  
   - 解析失败率超过10%时，应停止使用SPU维度分析，改为SKU粒度，并反馈数据问题。

### 1.3 通用属性维度拆解

不同品类的关键属性不同。属性分析仅针对能够从SKU或`product-name`中可靠解析的属性执行。

**属性提取优先级：**
1. **若属性需从SKU中提取**：必须已在1.2.0步骤确认SKU规则，并按规则解析。  
2. **若SKU规则未确认，或属性不在SKU中**：优先从`product-name`中通过关键词提取（如标题中的“红色”、“128G”、“M码”等）。  
3. **两者均无法提取时**：该属性不单独分析，不做猜测性拆分。

常见品类可解析属性示例：

| 品类 | 常见可解析属性 |
|---|---|
| 服装/鞋靴 | 颜色、尺码、版型、材质 |
| 箱包 | 颜色、尺寸、款式 |
| 家居/厨具 | 规格、尺寸、颜色、材质 |
| 美妆/个护 | 色号、规格、容量、肤质适用 |
| 消费电子 | 型号、容量、颜色、版本、规格 |
| 运动户外 | 尺寸、颜色、规格、重量 |
| 母婴/玩具 | 适用年龄、尺寸、颜色、型号 |

**执行原则：**
- 每个属性维护映射字典或正则规则，作为备选工具。  
- 无法识别或非该品类关键属性的，标记为「未知」，不单独分析。  
- 若某品类没有可解析属性，则跳过属性维度分析，不强行拆分。

### 1.4 处置结果归类

1. **映射规则**  
   优先以`detailed-disposition`字段为准；若该字段为空，再使用`status`字段。

2. **可售/不可售映射表**  

| 最终归类 | 原始处置/状态示例 |
|---|---|
| 可售退货 | `SELLABLE`、`Unit returned to inventory` |
| 不可售-客户损坏 | `CUSTOMER_DAMAGED`、`DAMAGED_BY_CUSTOMER` |
| 不可售-商品缺陷 | `DEFECTIVE`、`PRODUCT_DEFECT` |
| 不可售-其他 | `CARRIER_DAMAGED`、`IMMEDIATE_LIQUIDATION`、`IMMEDIATE_DONATION`、`UNSAFE`、`EXPIRED` 等 |
| 未明确 | 字段为空或无法匹配 |

3. **新增字段**  
   在数据中新增`loss_type`字段，取值：`可售退货`、`不可售-客户损坏`、`不可售-商品缺陷`、`不可售-其他`、`未明确`。

---

## 二、整体大盘概览分析

### 2.1 总量与时间分布

1. 统计总退货单量、总退货件数、平均每单退货件数。  
2. 若数据跨多天，按日、周、月生成退货件数趋势图，标注异常峰值日期。  
3. 按`fulfillment-center-id`统计各仓库退货件数及占比。  
4. 观察是否存在单日异常、单仓库异常集中现象。

### 2.2 系统退货原因分布

1. 按`reason`字段统计退货件数、占比、累计占比，从高到低排序。  
2. `Other`、`Unspecified`、空值单独统计，若合计占比超过10%，需在报告中说明数据质量风险。  
3. 输出Top3核心系统原因，作为整体退货主导因素。

### 2.3 处置结果分布

1. 统计可售退货、不可售退货的件数与占比。  
2. 不可售退货中，按`loss_type`拆分：客户损坏、商品缺陷、其他。  
3. 输出不可售件数占比，作为整体损失程度的核心指标。

---

## 三、品类维度分析

### 3.1 品类退货规模与结构

1. 统计各一级品类的退货件数、退货占比。  
2. 对每个品类统计退货单量、件数、平均每单件数。  
3. 设置最小样本量：若某品类退货件数 < 30，标记为「样本不足」，不进行深度归因。

### 3.2 品类退货原因差异

1. 对每个品类拆解退货原因分布，输出Top3原因。  
2. 比较不同品类的核心退货动因差异，例如：  
   - 电子品类：更多为功能缺陷、设置复杂、兼容性问题。  
   - 服装品类：更多为尺码、版型、面料问题。  
   - 家居品类：更多为尺寸不符、材质不符、安装问题。  
3. 输出「品类-原因结构表」。

### 3.3 品类不可售损失

1. 对每个品类拆解可售/不可售占比。  
2. 统计各品类不可售件数、不可售率。  
3. 识别「高退货规模 + 高不可售率」品类，作为优先改善对象。

---

## 四、产品/SPU维度分析

### 4.1 高退货产品识别

1. 若已按确认规则提取SPU/父体/型号，则按SPU统计退货件数，输出Top10高退货产品。  
2. 若无法可靠聚合SPU，则按SKU粒度统计，输出Top10高退货SKU。

### 4.2 高损失产品识别

1. **不可售件数Top10**：定位实际库存损失最高的产品。  
2. **不可售率Top10**：在退货件数 ≥ 20 的产品中，按不可售率排序，定位低量但高损失产品。

### 4.3 问题产品归因

对重点产品拆解：
- 核心退货原因分布。  
- 可售/不可售占比。  
- 客户评论高频问题标签。  
- 主要属性分布（如颜色、尺码、容量等）。  

输出「退货量高 + 不可售率高」的双高风险产品清单，作为重点优化对象。

---

## 五、属性维度分析（按品类适用）

### 5.1 属性选择与解析

1. 根据品类选择1-3个关键属性，例如：  
   - 消费电子：容量、颜色、型号。  
   - 美妆：色号、规格。  
   - 家居：尺寸、材质。  
2. 使用1.3节规则解析属性。若属性需从SKU提取但SKU规则未确认，则该属性不单独分析。

### 5.2 属性退货分布

1. 按属性值统计退货件数与占比。  
2. 对每个属性值计算退货原因分布。  
3. 观察是否存在某属性值退货占比异常高的情况。

### 5.3 属性与原因交叉

1. 交叉「属性 × 退货原因」，重点查看：  
   - 颜色维度：`DID_NOT_LIKE_COLOR`、色差相关评论是否集中在特定颜色。  
   - 尺码/尺寸维度：`TOO_SMALL`、`TOO_LARGE`、尺寸不符是否集中在特定尺码/规格。  
   - 容量/版本维度：功能不符、型号错误是否集中在特定容量/版本。  
2. 将属性退货原因与整体原因对比，判断是否存在属性特异性问题。

### 5.4 属性与损失交叉

1. 计算各属性值的不可售率。  
2. 输出「属性-不可售率-核心原因」表，定位高损失属性。  
3. **注意**：无销量数据时，属性退货占比高可能与该属性销量高有关，不可直接归因为该属性本身问题，需结合评论综合判断。

---

## 六、客户评论语义深度归因

### 6.1 评论清洗与有效筛选

1. 去除纯数字、纯符号、无意义单字或短语：`ok`、`yes`、`no`、`na`、`none`、`n/a`。  
2. 保留包含关键信息的短句，例如：`stopped working after 2 days`、`wrong size`、`not as described`。  
3. 对非英语评论，可单独标记语言类型；若数量较少，可人工翻译后分析，或暂不纳入语义分析。  
4. 有效评论占总评论比例应在报告中注明。

### 6.2 通用评论标签体系

采用「关键词规则 + 人工抽检」方式建立通用评论标签。每条评论分配1个主标签，必要时可加1个辅助标签。

| 一级标签 | 二级细分标签 | 评论示例 |
|---|---|---|
| 商品质量问题 | 功能故障、做工粗糙、材质差、易损坏、有异味、配件缺失 | `stopped working`、`cheap material`、`missing parts` |
| 商品与描述不符 | 颜色色差、尺寸不符、型号错误、功能不符、外观不符 | `wrong color`、`not as described`、`smaller than expected` |
| 规格/尺寸问题 | 偏大、偏小、容量不足、尺寸不合、版型不合 | `too small`、`too large`、`doesn't fit` |
| 外观/设计问题 | 不喜欢外观、颜色不喜欢、设计不合理、不美观 | `did not like color`、`ugly design` |
| 使用体验问题 | 操作复杂、不舒适、效果差、噪音大、续航差 | `hard to use`、`not comfortable`、`battery drains fast` |
| 物流与错发 | 发错货、漏发、配送延迟、包装破损、货不对板 | `wrong item received`、`missing item`、`arrived late` |
| 主观不想要 | 买错了、不需要了、计划改变、买了其他款 | `no longer needed`、`changed my mind` |

**标签互斥优先级：**
`物流与错发 > 商品质量问题 > 商品与描述不符 > 规格/尺寸问题 > 使用体验问题 > 外观/设计问题 > 主观不想要`

> 示例：评论同时提到“wrong item”和“not working”，优先归入“物流与错发”。

### 6.3 与系统原因交叉验证

1. 将系统`reason`映射到上述评论标签大类，形成映射表。  
2. 统计系统原因与评论主标签的一致性。  
3. 输出「一致性矩阵」：行为系统原因，列为评论标签。  
4. 不一致修正规则：  
   - 当评论明确指向商品问题（质量、功能、尺寸、色差、发错货等），而系统原因为宽泛类（如`UNWANTED_ITEM`、`OTHER`）时，将该订单归入评论标签。  
   - 否则保留系统原因。  
5. 输出「修正后退货原因分布」，并注明修正比例。

**重要说明：**  
评论未提及不等于问题不存在，因此此处只能称为“一致率/修正率”，不能称为“系统分类准确率”。

---

## 七、处置结果交叉专项分析

### 7.1 可售退货动因分析

1. 筛选所有可售退货，统计系统原因分布与评论标签分布。  
2. 核心关注：  
   - 主观不想要、尺寸/规格不合适、外观不喜欢等不影响商品本身的因素占比。  
   - 是否存在“可售但评论指向质量问题”的订单，这类订单可能因检查不严而错误入库，需反馈仓库质检环节。

### 7.2 不可售退货动因分析

1. 筛选所有不可售退货，统计系统原因分布与评论标签分布。  
2. 按`loss_type`拆分：  
   - **客户损坏类**：用户试用或退回过程中造成的损坏。  
   - **商品本身问题类**：出厂缺陷、做工问题、质量问题。  
   - **其他类**：运输损坏、仓库操作损坏等。  
3. 定位导致商品不可售的Top3核心问题，并关联到具体品类/产品。

### 7.3 损失交叉矩阵

1. 计算各维度的不可售率：  
   `不可售率 = 不可售件数 / 该维度退货件数`  
2. 按品类、产品/SPU、关键属性分别计算不可售率，设置最小样本量。  
3. 输出「高损失归因矩阵」：维度 × 不可售率 × 核心原因，用于确定改善优先级。

---

## 八、分析输出规范

最终分析报告按以下结构输出：

1. **核心摘要**  
   - 3-5条核心发现。  
   - 每条发现对应一个建议方向。

2. **整体退货概览**  
   - 总量、趋势、核心原因、可售/不可售占比。

3. **品类维度退货分析**  
   - 各品类退货规模、原因结构、损失分布。

4. **高退货/高损失产品分析**  
   - 退货量Top10、不可售件数Top10、不可售率Top10。  
   - 重点产品问题归因。

5. **关键属性专项分析**  
   - 按品类适用属性分析（颜色、尺码/规格、容量等）。  
   - 属性退货集中度、与原因/损失的交叉结论。

6. **客户评论真实问题洞察**  
   - 评论标签分布、与系统原因对比、修正后退货原因。

7. **不可售损失专项归因**  
   - 客户损坏 vs 商品缺陷 vs 其他。  
   - 导致不可售的Top3问题。

8. **数据质量与局限性说明**  
   - 字段缺失率、SKU解析失败率（如未确认规则则标注“未解析”）、未分类占比。  
   - 无销量/成本数据，结论以内部结构为主。

9. **行动建议清单**  
   - 按优先级排序，明确责任方向：  
     - 品控/质量缺陷 → 质检/供应链。  
     - 尺码/规格偏差 → 产品定义/详情页优化。  
     - 描述不符 → 图片/文案/规格参数优化。  
     - 包装/运输损坏 → 包装改进/物流协商。  
     - 功能/使用体验问题 → 产品设计/说明优化。  
     - 主观不想要 → 页面信息透明度、用户预期管理。

---

## 附录（示例）

### 附录A：系统退货原因映射模板

| 系统Reason | 标签大类 |
|---|---|
| `DEFECTIVE` | 商品质量问题 |
| `DAMAGED` | 商品质量问题/物流损坏 |
| `NOT_AS_DESCRIBED` | 商品与描述不符 |
| `WRONG_ITEM` | 物流与错发 |
| `MISSING_PARTS` | 商品质量问题 |
| `NO_LONGER_NEEDED` | 主观不想要 |
| `DID_NOT_LIKE_STYLE` | 外观/设计问题 |
| `DID_NOT_LIKE_COLOR` | 外观/设计问题 |
| `TOO_SMALL` / `TOO_LARGE` | 规格/尺寸问题 |
| `ARRIVED_TOO_LATE` | 物流与错发 |
| `OTHER` | 未分类 |

> 具体映射可根据实际系统原因字段进行调整。

### 附录B：处置结果映射模板

| 原始处置/状态 | 最终归类 |
|---|---|
| `SELLABLE` | 可售退货 |
| `Unit returned to inventory` | 可售退货 |
| `CUSTOMER_DAMAGED` | 不可售-客户损坏 |
| `DEFECTIVE` | 不可售-商品缺陷 |
| `CARRIER_DAMAGED` | 不可售-其他 |
| `IMMEDIATE_LIQUIDATION` | 不可售-其他 |
| `IMMEDIATE_DONATION` | 不可售-其他 |
| `EXPIRED` | 不可售-其他 |

> 关键词库需根据实际商品扩展。

### 附录C：属性解析规则模板（仅作示例，实际以确认为准）

**重要：** 本附录仅作为属性解析规则的示例模板，实际执行时必须结合**已确认的SKU规则**或**标题关键词规则**使用，不得直接照搬。

| 属性 | 常见位置 | 正则/字典示例 |
|---|---|---|
| 颜色 | SKU中段或标题 | `BK`→Black、`WH`→White、`RD`→Red |
| 尺码/尺寸 | SKU末尾或标题 | `S`、`M`、`L`、`XL`、`10x10cm` |
| 容量 | SKU中段或标题 | `128G`、`256G`、`500ml`、`1L` |
| 型号/版本 | SKU前缀或标题 | `AC-2245`、`V2`、`2023` |
| 材质 | 标题 | `Cotton`、`Stainless Steel`、`Silicone` |

> 实际使用前，必须与用户确认这些属性在SKU或标题中的具体位置和编码规则。

