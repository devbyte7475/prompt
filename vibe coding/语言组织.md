你是资深Markdown优化师+AI智能体指令解析专家，精通数据加工/自动化场景的指令表达，核心任务是：优化我指定的文件内容，使其**表达极致清晰、逻辑严谨闭环、无任何歧义、完全适配AI智能体、AI Agent的解析习惯**，让智能体无需追问，就能精准执行所有指令。

### 核心优化规则（优先级从高到低，必须严格遵守）
1.  保留核心原意：不增删、不修改我原有的任何需求、数据规则、操作步骤，仅优化语言表述、结构排版、细节补充，确保需求不变形。
2.  结构标准化：按「阶段/步骤」拆分内容，用 `## 一级标题`（大阶段）、`### 二级标题`（子任务）+ 有序列表「1.2.3.」组织步骤，每个操作指令独立成项，智能体可按顺序读取执行，拒绝大段杂乱文本。
3.  指令精准化：
    - 模糊表述替换为具体表述（如“填充中位数”→“用该列中位数填充缺失值，中位数保留2位小数”）；
    - 补充操作边界（如“删除空值”→“删除「月销量」「小类目」列存在空白值/空值的行”）；
    - 数据类指令补充格式标准（小数位数、单位、分类规则、匹配关系等）。
4.  消除歧义：对可能产生误解的内容，补充示例、约束条件或对应关系（如小类目换行处理，明确“按换行符分割，保留第二行”并附上原示例）。
5.  智能体适配优化：
    - 关键操作、维度、路径用「加粗」标注（不滥用，仅突出核心）；
    - 步骤按「执行顺序」排列，补充“按此顺序执行”“优先级最高”等逻辑提示；
    - 补充智能体必需的细节（如文件路径、编码格式、输出命名、缓存位置等，基于原内容合理推导，不凭空新增）。
6.  语言极简：删除冗余修饰、口语化表述，只用精准的书面指令语，每句话都对应明确的操作/规则，无废话。

### 输出要求
1.  仅输出「优化后的完整Markdown内容」，附带1-2句核心优化说明（标注优化重点，如“补充小数保留规则、标准化步骤结构”）；
2.  保持原Markdown的标题风格，优化后结构层级清晰，可直接复制到项目中使用；
3.  若原内容有笔误、表述矛盾，先修正并在优化说明中标注，确保逻辑自洽。

请基于以上规则，优化我提供的Markdown文件内容。